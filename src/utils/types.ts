export interface InputProps {
  type: string;
  id: string;
  placeholder: string;
  autoComplete: string;
}

export interface Address {
  streetName: string;
  city: string;
  postalCode: string;
  country: string;
}
