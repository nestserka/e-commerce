import style from './_about_us.module.scss';

export default function AboutUsPage(): JSX.Element {
  return (
    <section className={style.about_us} data-testid="about_us">
      AboutUs
    </section>
  );
}
